pip install requests pypdf ebooklib beautifulsoup4 fastapi starlette pydub python-multipart uvicorn

✅ macOS
brew install ffmpeg
✅ Ubuntu / Debian
sudo apt update
sudo apt install ffmpeg
✅ Windows
Install FFmpeg:
1️⃣ Download from: https://www.gyan.dev/ffmpeg/builds/
2️⃣ Extract & add bin folder to PATH
3️⃣ Verify:
ffmpeg -version

python3 -m piper.download_voices en_US-lessac-medium

source venv/bin/activate

python3 -m piper.http_server -m en_US-lessac-medium --port 5001

python3 -m gunicorn app:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:5002 --timeout 600